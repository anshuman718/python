import pyautogui

while True:
    h = pyautogui.position()

    print(h)

# pyautogui is a python module used for automatic interaction with computer.It uses different coordinates on the screen to move the mouse pointer.
# Here we have used it to find the current position of the mouse on screen.As we move the mouse position also changes.